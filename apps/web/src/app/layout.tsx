import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'StoreToStove - AI-Powered Recipe Planning & Smart Shopping',
  description:
    'Get AI-generated recipe ideas, manage shopping lists, and see exactly how much each meal costs at nearby supermarkets. Cook smarter, save money.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
